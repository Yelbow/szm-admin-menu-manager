# TODO

- Onbevestigd — controleer op een echte WordPress-testsite:
  - Update (niet fresh-activate) van een oudere versie: verschijnt `minimale_editor` onder Users → Roles zonder dat het plugin handmatig gereactiveerd is?
  - Na activatie: verschijnt de "Install Required Plugins"-melding met User Role Editor als optie?
  - Test dat een `minimale_editor`-gebruiker via de Site Editor (FSE) bij Header & Footer kan.
- Openstaande vraag, nog niet beantwoord: `publish_posts` staat niet in de meegegeven capability-lijst voor `minimale_editor`, terwijl `edit_posts`/`create_posts` er wel in staan — berichten blijven dus op concept/pending staan. Navragen of dat bewust is.
- Onbevestigd (2.3.0, profielen-herbouw) — nog niet getest op een echte site, alleen `php -l` gedaan:
  - Upgrade-migratie: een site die nog op de oude platte config (2.2.0 en eerder) staat, moet na deze update automatisch één profiel `profile_migrated` krijgen met dezelfde instellingen als voorheen — ongetest.
  - Overlappende profielen: twee profielen met een gedeelde rol, met verschillende titel/icoon voor dezelfde menu-slug — checken dat het profiel met de minste rollen wint, en bij gelijke rollen-aantal het eerst genoemde profiel.
  - `allow_admin_editing` aanzetten, jezelf (Administrator) in een profiel zetten met een beperkte allow-list, opslaan, en verifiëren: (a) alleen de allow-listed items + Dashboard/Profile/Plugins/Settings blijven zichtbaar, (b) op de instellingenpagina zelf zijn nog steeds ALLE menu-items te kiezen (niet alleen de al-toegestane), zodat je een eerder verborgen item kan terugzetten.
  - "+ Add profile" / "Remove profile" knoppen in de browser: cloning met unieke keys, submit, en of de nieuwe/verwijderde profielen goed in `wp_options` terechtkomen na sanitizing.
- Jij (Jelle) gaf aan: "verify when done if it works for me" — dat vereist een admin-account op een echte WordPress-site; kan hier niet worden uitgevoerd. Zie bovenstaande checklist.
- 2.4.0 (tabs + vast Administrator-profiel) — geverifieerd op fse-test-wp via wp-cli `eval`/`eval-file` (géén browserklik):
  - ✅ `szm_amm_get_settings()` injecteert `profile_administrator` altijd, met rol hard op `administrator`.
  - ✅ `szm_amm_sanitize_settings()`: admin-profiel niet verwijderbaar (wordt teruggezet als het ontbreekt in de POST), rollen niet aanpasbaar (blijft `["administrator"]` ook als iemand iets anders instuurt), label wél aanpasbaar; `administrator` kan niet meer in een ander profiel terechtkomen.
  - ✅ `szm_amm_get_matching_profiles()`: met toggle aan matcht een Administrator-gebruiker alleen `profile_administrator`; met toggle uit matcht niets (ongemoeid, zoals voorheen).
  - ✅ `szm_amm_render_settings_page()` rendert zonder fatale fouten en bevat de tab-knoppen + het admin-profiel + de "locked"-tekst bij de floor-items.
  - ❌ Nog NIET getest: het daadwerkelijke tab-gedrag in de browser (klikken, "+ Add profile", "Remove profile", live label-sync op de tab) — dat is JS en kan alleen met een echte klik in de browser geverifieerd worden, niet via wp-cli.
- 2.4.1: "+ Add profile" verplaatst van los knopje naar laatste tab in de tab-rij zelf (blijft altijd laatste dankzij `insertBefore` in JS). Alleen server-side geverifieerd (gerenderde HTML-positie via wp-cli) — het meeklikken/laatste-blijven-na-toevoegen is browser-only, nog niet gezien.
- Testomgeving (buiten deze repo, voor volgende sessie): `fse-test-wp`/`fse-test-db`/`fse-test-wpcli` containers zijn gestart (stonden stil) en draaien nu — host-pad `/home/jelle/Projects/fse-test/docker-site`. Plugin staat daar gekopieerd (niet symlinked) in `wp-content/plugins/szm-admin-menu-manager` — bij een volgende codewijziging opnieuw `rsync` nodig, geen auto-sync. `cyberpunk-admin`/`cyberpunk-login` mu-plugins zijn uitgezet (hernoemd naar `.php.disabled` in `wp-content/mu-plugins/`) op verzoek — hernoem terug om ze weer aan te zetten. `wp-cli-login-command` package is geïnstalleerd in de `fse-test-wpcli` container voor magic-login-links (`wp login create <user>`).
